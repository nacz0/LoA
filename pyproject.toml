[project]
name = "loa"
version = "0.1.0"
description = "Lightweight local AI agent orchestrator for local and LAN-hosted LLMs."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
loa = "loa.cli:main"

[tool.unittest]
start-directory = "tests"
